# Multi-file Profile payloads

For the HolyJS demo and the upload path, `process-profile` ingests many client-uploaded Profile payload files (compressed base64 `.txt`) under `profiles/`. Batch DB exports are out of scope for this repo’s CLI going forward — the talk and the app both speak the same wire format.
